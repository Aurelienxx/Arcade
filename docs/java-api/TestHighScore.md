# TestHighScore.java

 # TestHighScore

## Description générale

TestHighScore est une classe Java qui permet de tester la classe `HighScore` dans le cadre d'un environnement de jeu en 2D. Elle utilise des classes et méthodes du package MG2D pour gérer les fenêtres, le clavier, les textures, les réseaux d'arcades, etc.

## Attributs principaux

Le code fourni ne mentionne pas d'attributs principaux dans cette classe.

## Méthodes publiques avec leurs descriptions

`public static void main(String[] args)` : Méthode principale de l'application qui initialise et exécute le programme.

## Exemple d'utilisation si applicable

Le code fourni ne semble pas nécessiter d'exemple d'utilisation particulier car il semble utiliser les méthodes de classes externes, comme Fenetre, ClavierBorneArcade et HighScore. La classe `TestHighScore` sert simplement à initialiser les composants nécessaires pour lancer la classe `HighScore`.